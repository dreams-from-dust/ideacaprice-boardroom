{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "Public and private metadata for boardroom members.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "Unique identifier matching the Firebase Authentication UID."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "Primary email of the board member."
        },
        "displayName": {
          "type": "string",
          "description": "User display name."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when account was first initialized."
        }
      },
      "required": ["userId", "email"]
    },
    "Debate": {
      "title": "Venture Debate Session",
      "description": "A saved historical debate stream and strategic outcome.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Inherent identifier of the session."
        },
        "userId": {
          "type": "string",
          "description": "UID of the owner who requested the debate."
        },
        "idea": {
          "type": "string",
          "description": "The target venture or business model being stress tested."
        },
        "messages": {
          "type": "array",
          "description": "Complete sequential boardroom dialog feeds transcript."
        },
        "report": {
          "type": "object",
          "description": "The structural StrategyReport verdict and analysis generated by the Boss."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of the board convening event."
        }
      },
      "required": ["id", "userId", "idea", "messages", "report", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Individual member storage directory."
    },
    "/debates/{debateId}": {
      "schema": "Debate",
      "description": "Venture stress testing record indexing."
    }
  }
}
